---
description: Something to read while it thinks - a table for whatever this session is already working on
---

Deal a WAITLAYER table for what this session is actually doing right now.

Read the conversation so far, put the current job into one plain sentence — the
work itself, not the last thing said — and call the `convene_council` tool on
the `waitlayer` MCP server with it.

Say which sentence you used, so the person can see what the table was dealt for.

Then, exactly as with `/waitlayer:council`: the refusal first if one came back,
then the three cards with their install lines and the scout that played each,
then what they hold against each other.

Install nothing. If the `waitlayer` server is not connected, say so and stop.
