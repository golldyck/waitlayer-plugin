---
description: Deal three real MCP servers for what you are about to do, with the argument for each
argument-hint: [what you are about to do]
---

Convene the WAITLAYER council for this task:

$ARGUMENTS

Call the `convene_council` tool on the `waitlayer` MCP server with that task.

Then report what came back, in this order and no other:

1. **If the answer carries a no-fit verdict, say that FIRST, in its own words.**
   Two independent model houses have to agree before the layer refuses, so a
   refusal is the most expensive thing it can say — do not soften it, do not
   bury it under the cards, and do not go looking for a fourth option to make
   the table look better. "No server in the registry does this job" is a
   complete answer, and the three cards below it are only the closest it holds.
2. The three cards: the server's name, its registry id, and the install line
   exactly as given. Name the scout that played each one and keep its argument
   to the one sentence it made.
3. What the scouts hold against each other, if anything came back.

Do not install anything. Do not run the install lines. This command deals a
table; the person reading it decides whether to sit down.

If the `waitlayer` server is not connected, say so plainly and stop — do not
substitute your own recollection of which MCP servers exist. The point of the
layer is that the cards come from the live registry, not from memory.
